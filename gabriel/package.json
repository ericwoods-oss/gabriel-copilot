{
  "name": "gabriel-copilot",
  "private": true,
  "version": "1.0.0",
  "description": "Gabriel — AI Insurance Agent Copilot",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "gh-pages": "^6.3.0"
  }
}
